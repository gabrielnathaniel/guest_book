import 'package:flutter/material.dart';

class GuestDetailScreen extends StatelessWidget {
  static const routeName = '/guest-detail';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
